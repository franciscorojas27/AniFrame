export type Anime = {
    id: string;
    cap: number;
    name: string;
    slug: string;
    urlImg: string;
};

export type FavoriteItem = {
    id: number;
    name: string;
    slug: string;
    imgUrl: string;
};
